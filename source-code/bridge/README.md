# Bridge

## Indice

* [Dipendenze](#dipendenze)
* [Scripts](#scripts)

## Dipendenze

Il bridge è stato costruito con le dipendenze definite nel `package.json`. Installarle tramite il comando `npm install`.

L'interprete utilizzato è `node 22.19.0`.

## Scripts

Per eseguire il bridge, è stato predisposto un [package.json](./package.json). Gli script definiti sono i seguenti:

- `npm start` o `npm run start` esegue il progetto.
